{
  "name": "syndeocare",
  "private": true,
  "version": "1.0.0",
  "description": "SyndeoCare — Healthcare Staffing Platform for Yemen",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:e2e": "turbo test:e2e",
    "format": "prettier --write .",
    "clean": "turbo clean && rm -rf node_modules",
    "typecheck": "turbo typecheck",
    "boundaries": "turbo boundaries"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.3.0",
    "@changesets/cli": "^2.27.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
